package com.vjhkfuct.replaysablecompat;

import net.neoforged.fml.common.Mod;
import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

@Mod(ReplaySableCompat.MOD_ID)
public final class ReplaySableCompat {
    public static final String MOD_ID = "replaysablecompat";
    public static final Logger LOGGER = LogManager.getLogger(MOD_ID);

    public ReplaySableCompat() {
    }
}
